#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - Returns the length of a string.
 * @s: the string to get the length of
 * Return: the length of @s.
 */

int _strlen(char *s)
{
	int length = 0;

	length = strlen(s);
	return (length);
}
