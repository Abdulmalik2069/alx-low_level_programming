#include "main.h"

/**
 * mul - multiplication of two integers
 * @a: the fist number to multiply
 * @b: second number to multiply with the first
 * Return: the result of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
