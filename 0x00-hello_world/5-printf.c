#include <stdio.h>

/**
 * main - it serves as a entry point of the program where the execution begins
 *
 * Return: 0 when successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
